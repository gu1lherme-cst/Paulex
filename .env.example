# Copie este arquivo para ".env" e preencha com os dados do seu projeto Supabase
# (Supabase → Project Settings → API).
#
# A VITE_SUPABASE_ANON_KEY é uma chave pública por design — pode ficar no
# frontend. NUNCA use a "service_role key" aqui.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
